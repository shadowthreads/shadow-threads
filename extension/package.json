{
  "name": "shadow-threads-extension",
  "version": "0.2.0",
  "description": "Shadow Threads 浏览器扩展 - 在任意 LLM 页面创建影子子线程",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "clean": "rimraf dist/*"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.30.0",
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}